# Hebrew and Encoding

Use Unicode end to end and declare encodings at file/network boundaries. Test Hebrew text, mixed RTL/LTR identifiers, punctuation, normalization, CSV delimiters and round trips through the actual integration path.

Do not normalize business identifiers without a verified requirement. Compare semantic values where presentation direction differs from stored order.
