# Logging

Prefer structured logs with event name, sanitized correlation ID, component, outcome, duration and stable error category. Redact authorization headers, tokens, credentials, connection strings, personal data and customer payloads.

Log retries and final outcomes without duplicating sensitive bodies. Document retention and access expectations in the project when logs contain operational metadata.
