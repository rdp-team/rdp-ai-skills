# מתחילים כאן — RDP

1. חלצו את כל ה־ZIP לתיקייה חדשה. אין צורך להעתיק Skills ידנית.
2. פתחו את התיקייה `rdp-ai` ב־Claude Code או ב־Codex כסביבת עבודה.
3. כתבו לסוכן: **״חבר אותי לפרויקט של RDP״**.
4. אשרו התחברות בחלון הרשמי של GitHub אם צריך, ובחרו פרויקט קיים או חדש.
5. פתחו את תיקיית הפרויקט שהוכנה וכתבו מה אתם רוצים לבצע.

הסוכן קורא את קובצי ההתחלה ומפעיל את הכלים. פעולת ההתחברות היא אישית; לא שולחים סיסמה או Token בצ'אט. אם הסוכן אינו קורא את החבילה אוטומטית, כתבו: ״קרא את START_HERE.md ואת skills/rdp-onboarding/SKILL.md ופעל לפיהם״.

## מה צריך במחשב

Git, GitHub CLI, Python בגרסה 3.11 ומעלה, ו־Claude Code או Codex מחובר לחשבון מורשה. החבילה אינה מתקינה את יישום הסוכן ואינה כוללת מנוי למודל. המתקין עצמו משתמש רק בספרייה הסטנדרטית של Python.

ב־Windows אפשר להשתמש ב־`py -3`; ב־macOS לעיתים הפקודה היא `python3`. בהוראות להלן `python` הוא אותו מפרש מותקן.

## פקודות שהסוכן יכול להריץ

```text
python rdp.py doctor --offline
python rdp.py doctor
gh auth login --hostname github.com --web
gh auth setup-git
python rdp.py onboard --repo rdp-team/PROJECT --destination PATH
```

התחברות מבצעים רק אם הבדיקה מצאה שהיא חסרה. לפרויקט חדש מוסיפים `--create`; לפרויקט Priority מוסיפים `--priority`. מחליפים PROJECT ו־PATH בבחירת העובד. אין להריץ את הדוגמאות עם שמות הדוגמה כפי שהם.

ההתקנה יוצרת ענף `codex/rdp-onboarding`. הסוכן משלים את מסמכי הפרויקט ופקודות הבדיקה, מכין Commit ופותח PR. אין Merge אוטומטי.

## שימוש יומיומי

״טפל ב־Issue מספר 12״, ״הוסף ייצוא לאקסל לפי האפיון״, ״הכן את העבודה למסירה לדנה״ או ״הצע לשתף את הפתרון כ־Skill ארגוני״.

מידע לקוח נשאר בפרויקט. מקבלים גרסאות חדשות מה־Releases הפרטיים של RDP; עדכון של פרויקט פעיל עובר PR. ZIP מועבר רק לעובדים מורשים.
